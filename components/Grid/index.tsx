import {
  Container as RBContainer,
  Row as RBRow,
  Col as RBCol,
} from "react-bootstrap";
import { styled } from "../../stitches.config";

const Container = styled(RBContainer);
const Row = styled(RBRow);
const Col = styled(RBCol);

export { Container, Row, Col };
